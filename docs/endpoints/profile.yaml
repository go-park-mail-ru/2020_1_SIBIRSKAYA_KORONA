paths:
  /profile/{id}:
    get:
      tags:
      - "profile"
      summary: "Запрос к публичным данным пользователя по id в сервисе"

      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: id пользователя, публичные данные которого хотим просмотреть
          example: 1106234 
          
      responses:
        200:
          description: "Корректный запрос, существующий пользователь, данные в теле ответа"

          content:
            application/json:
              schema:
                allOf:
                - $ref: "../models/user.yaml#/components/schemas/User"
                - $ref: "../models/user.yaml#/components/schemas/ApiResponse"
              example: {  
                "name": "Vasya",
                "surname": "Pupkin",
                "nickname": "Legolas1",
                "email": "VasyaPupkin@gmail.com",
                "avatar": "Обсуждаем",
              }
  /profile/{nickname}:
    get:
      tags:
      - "profile"
      summary: "Запрос к публичным данным пользователя по его никнейму"
      description: "Получить можем "

      parameters:
        - in: path
          name: nickname
          required: true
          schema:
            type: string
          description: Ник пользователя, публичные данные которого хотим просмотреть
          example: VasyaSuper
          
      responses:
        200:
          description: "Корректный запрос, существующий пользователь, данные в теле ответа"

          content:
            application/json:
              schema:
                allOf:
                - $ref: "../models/user.yaml#/components/schemas/User"
                - $ref: "../models/user.yaml#/components/schemas/ApiResponse"
              example: {
                "name": "Vasya",
                "surname": "Pupkin",
                "nickname": "Legolas1",
                "email": "VasyaPupkin@gmail.com",
                "avatar": "Обсуждаем",
              }            